CREATE TABLE address_object
(
    id           VARCHAR(255),
    object_id    VARCHAR(255),
    object_guid  VARCHAR(255),
    change_id    VARCHAR(255),
    name         VARCHAR(255),
    type_name    VARCHAR(255),
    level        VARCHAR(255),
    oper_type_id VARCHAR(255),
    prev_id      VARCHAR(255),
    next_id      VARCHAR(255),
    update_date  VARCHAR(255),
    start_date   VARCHAR(255),
    end_date     VARCHAR(255),
    is_actual    VARCHAR(255),
    is_active    VARCHAR(255)
);